# تقرير هندسي: تحليل بنية بيانات منصة أكاديمية مسار

## نطاق التقرير

هذا التقرير **قرائي وتحليلي فقط**. تم فحص مخطط Supabase والبيانات الحالية باستخدام استعلامات قراءة محدودة. لم يتم إنشاء أي جدول، ولم يتم تعديل أو نقل أو حذف أي سجل، ولم يتم تغيير Schema أو AdminStudentWeb أو أي كود تشغيلي.

> **النتيجة المختصرة:** البنية الحالية مناسبة كبداية صغيرة لكتالوج كورسات، لكنها لا تمثل الرؤية المستقبلية `Stage → Subject → Teacher → Course → Lesson` تمثيلاً علائقياً صحيحاً. أفضل مسار هو الاحتفاظ بالجداول الحالية، وإضافة طبقة أكاديمية normalized تدريجياً، مع إبقاء `courses.instructor` مؤقتاً للتوافق أثناء الانتقال.

## 1. نتائج التدقيق الفعلي في Supabase

تم العثور على أربع جداول عامة مرتبطة بنطاق المنصة: `admin_profiles` و`course_categories` و`courses` و`lessons`. العلاقات الموجودة فعلياً هي `courses.category_id → course_categories.id` و`lessons.course_id → courses.id`.

| المؤشر | النتيجة الحالية |
|---|---:|
| إجمالي الكورسات | 1 |
| الكورسات المنشورة | 1 |
| إجمالي الدروس | 1 |
| الدروس المنشورة | 1 |
| التصنيفات في `course_categories` | 0 |
| مجموعات المدرسين المكررة بعد التطبيع | 0 |

البيانات الحالية تتكون من كورس منشور واحد بعنوان **C++**، ومدرس محفوظ كنص هو **الزيرو أسامة**، وله درس منشور واحد بعنوان **الدرس الأول**. قيمة `category_id` للكورس الحالي هي `NULL`، وعدد الدروس المرتبطة به درس واحد، وعدد الدروس المنشورة المرتبطة به درس واحد. لا توجد حالياً قيمة فعلية يمكن استخدامها لربط هذا الكورس بصف أو مادة أو مدرس كيان مستقل.

| الجدول | الأعمدة المهمة التي تم فحصها | الملاحظة المعمارية |
|---|---|---|
| `admin_profiles` | `user_id`, `role`, `created_at` | جدول صلاحيات الإدارة، وليس جزءاً من شجرة المحتوى |
| `course_categories` | `id`, `name`, `slug`, `is_active` | تصنيف عام؛ لا توجد سجلات حالياً |
| `courses` | `category_id`, `title`, `instructor`, `is_published` | الكورس يحمل المدرس كنص، ولا يحمل Stage أو Subject أو Teacher FK |
| `lessons` | `course_id`, `title`, `youtube_url`, `youtube_video_id`, `sort_order`, `is_published` | يمثل الدروس المرتبة داخل الكورس بصورة مناسبة للرؤية الحالية |

لم يظهر تكرار فعلي في أسماء المدرسين ضمن البيانات الحالية، لكن ذلك يعود إلى وجود كورس واحد فقط. لذلك لا يمكن اعتبار الحقل النصي آمناً من التكرار مستقبلاً؛ فمثلاً `أحمد علي` و`الأستاذ أحمد علي` و`احمد علي` ستظهر كقيم مختلفة ما لم يتم تطبيعها داخل كيان مستقل.

## 2. مقارنة الرؤية المطلوبة مع Schema الحالية

الرؤية المطلوبة هي:

```text
Stage
  └── Subject
        └── Teacher
              └── Course
                    └── Lesson
                          └── YouTube Video
```

أما البنية الحالية فهي فعلياً:

```text
CourseCategory ──< Course ──< Lesson ── YouTube fields
                         └── instructor: text
```

| المستوى المطلوب | التمثيل الحالي | التقييم |
|---|---|---|
| Stage | غير موجود | فجوة أساسية؛ لا يمكن اختيار الصف أو ربط المحتوى به |
| Subject | غير موجود | فجوة أساسية؛ لا يمكن عرض مواد صف معين |
| Teacher | `courses.instructor` كنص | غير منظم، قابل للتكرار، ولا يدعم علاقات متعددة |
| Course | `courses` | يمكن الاحتفاظ به، مع إضافة FK أكاديمي لاحقاً |
| Lesson | `lessons` | يمكن الاحتفاظ به؛ العلاقة مع الكورس صحيحة |
| YouTube Video | `youtube_url` و`youtube_video_id` داخل `lessons` | مناسب للنسخة الحالية ولا يحتاج YouTube API لتحقيق الرؤية الأساسية |

## 3. هل البنية الحالية مناسبة؟

**هي مناسبة للـMVP الحالي، وليست كافية للرؤية المستقبلية.** يمكنها عرض كورسات عامة ودروس مرتبة، وتدعم تشغيل YouTube، لكنها لا تستطيع تمثيل سؤال أساسي مثل: «ما المواد الموجودة في الخامس العلمي؟» لأن الصف والمادة غير موجودين ككيانات أو علاقات. كما لا تستطيع تمثيل مدرس يدرّس مادة في صفوف متعددة دون تكرار النص داخل كل كورس.

الاستمرار في استخدام `courses.instructor` و`course_categories` لإخفاء هذه الفجوات سيؤدي إلى منطق خاص داخل التطبيق، وتكرار أسماء، وصعوبة في استيراد Playlists، وصعوبة في المزامنة المحلية، واحتمال اختلاف نفس المدرس أو المادة بين شاشات مختلفة.

## 4. ما الذي يمكن الاحتفاظ به؟

### `admin_profiles`

يبقى كما هو خارج شجرة المحتوى. دوره هو التحكم في الوصول إلى لوحة الإدارة وعمليات الإدارة، ولا ينبغي ربطه بالصفوف أو المواد أو المدرسين.

### `courses`

يبقى الكيان المركزي للكورس. الحقول الحالية مثل العنوان والوصف وحالة النشر والـslug مناسبة. يُفضّل إضافة FK أكاديمي nullable في مرحلة انتقالية بدلاً من حذف `instructor` فوراً.

### `lessons`

يبقى مرتبطاً بـ`courses` عبر `course_id`. حقلا `sort_order` و`is_published` مناسبان. حقلا `youtube_url` و`youtube_video_id` يمكن الاحتفاظ بهما؛ فالفيديو هنا مورد خارجي وليس جدولاً مستقلاً ضرورياً في هذه المرحلة.

### `course_categories`

الأفضل أن تبقى للتصنيفات العامة أو الوسوم، مثل «برمجة» أو «تطوير مهني» أو «مميز». لا يُنصح بإعادة تفسيرها على أنها Stage أو Subject، لأن التصنيف العام يجيب عن سؤال مختلف عن السؤال الأكاديمي: «إلى أي تصنيف ينتمي هذا الكورس؟» مقابل «لأي صف ومادة ومدرس ينتمي؟».

## 5. Schema مستقبلية موصى بها

### الكيانات الأساسية

| الكيان المقترح | الغرض | أهم الحقول |
|---|---|---|
| `stages` | الصف أو المرحلة التعليمية | `id`, `name`, `slug`, `is_active`, `sort_order` |
| `subjects` | المادة الدراسية | `id`, `name`, `slug`, `is_active`, `sort_order` |
| `stage_subjects` | المواد المتاحة في كل صف | `stage_id`, `subject_id`, unique على الزوجين |
| `teachers` | هوية المدرس الموحدة | `id`, `display_name`, `slug`, `is_active` |
| `teacher_assignments` | ربط مدرس بمادة وصف | `stage_subject_id`, `teacher_id`, unique على العلاقة |
| `courses` | الكورس | إضافة `teacher_assignment_id` nullable في البداية |
| `lessons` | الدرس المرتب | الاحتفاظ بالعلاقة الحالية مع `courses` |
| `course_categories` | تصنيفات عامة اختيارية | الاحتفاظ بها مستقلة عن الشجرة الأكاديمية |

### لماذا `stage_subjects` و`teacher_assignments`؟

قد تظهر المادة نفسها في أكثر من صف، وقد يدرّس المدرس نفسه أكثر من مادة أو صف. لذلك فإن علاقة `Stage → Subject` ليست بالضرورة علاقة ملكية مباشرة، بل علاقة إتاحة أو ربط. وبالمثل، فإن ربط المدرس بالصف والمادة يحتاج كياناً مستقلاً حتى لا تتكرر ثلاثة مفاتيح في كل كورس.

بذلك تصبح العلاقة المنطقية:

```text
stages
  └──< stage_subjects >── subjects
          └──< teacher_assignments >── teachers
                         └──< courses
                                  └──< lessons
```

ويظل `course_categories` مساراً موازياً اختيارياً:

```text
course_categories ──< courses
```

### هل نحتاج جدول `videos`؟

ليس في المرحلة الأولى. حقلا `youtube_url` و`youtube_video_id` داخل `lessons` كافيان للمشاهدة، ولا نحتاج إلى YouTube Data API أو تخزين فيديو. يمكن التفكير في جدول فيديو مستقل فقط إذا أصبح الدرس يحتوي عدة مصادر فيديو أو مصادر غير YouTube أو metadata مستقلة لكل مصدر.

## 6. أقل Migration ممكنة

أقل انتقال آمن لا يحطم البيانات الحالية هو **إضافة طبقة جديدة دون حذف القديمة**:

| المرحلة | العمل المقترح لاحقاً | أثره على البيانات الحالية |
|---|---|---|
| 0. توثيق ونسخ احتياطي | تثبيت snapshot وعدّادات وفحوص تكرار | لا تغيير |
| 1. إضافة الكيانات الأكاديمية | إنشاء `stages`, `subjects`, `stage_subjects`, `teachers`, `teacher_assignments` | لا تغيير في الجداول الحالية |
| 2. إضافة FK انتقالي | إضافة `courses.teacher_assignment_id` nullable | كل الكورسات الحالية تبقى صالحة حتى يتم ربطها |
| 3. تطبيع المدرسين | إنشاء Teacher من قيم `courses.instructor` بعد مراجعة بشرية | لا يُحذف النص الأصلي |
| 4. ربط المحتوى | ربط كل كورس بـ`teacher_assignment_id`، وتحديد Stage وSubject عند توفرهما | الكورس الحالي C++ يحتاج قراراً يدوياً بشأن الصف والمادة |
| 5. Dual Read | جعل Student App يقرأ الشجرة الجديدة مع fallback للكورسات غير المرتبطة | انتقال تدريجي بلا انقطاع |
| 6. تحديث Admin Web | جعل إضافة الكورس تختار Stage ثم Subject ثم Teacher Assignment | لا يبدأ إلا بعد اكتمال المرحلة السابقة |
| 7. القيود والتنظيف | فرض القيود بعد اكتمال الربط، ثم اعتبار `instructor` legacy | حذف الحقل ليس ضرورياً في البداية |

لا ينبغي تنفيذ أي backfill تلقائي للكورس الحالي قبل معرفة الصف والمادة الحقيقية؛ فقيمة `C++` واسم المدرس وحدهما لا يكفيان لاستنتاج Stage أو Subject بشكل موثوق.

## 7. ملاءمة الميزات المستقبلية

### اختيار الطالب للصف والمواد والمدرسين

تحتاج هذه الميزة إلى `stages` و`stage_subjects` و`teacher_assignments`. ستصبح الاستعلامات واضحة: اختيار Stage، ثم جلب Subjects المرتبطة به، ثم المدرسين المرتبطين بزوج Stage/Subject، ثم الكورسات التابعة لتكليف المدرس.

### استيراد YouTube Playlist وAI Course Import

يجب أن ينشئ الاستيراد كورساً مرتبطاً مسبقاً بـ`teacher_assignment_id`، ثم يضيف الدروس بالترتيب داخل `lessons`. لا ينبغي أن يعتمد الاستيراد على اسم المدرس النصي أو أن يخترع صفاً ومادة؛ إذا لم يتم اختيار العلاقة الأكاديمية، يجب أن يبقى الاستيراد في حالة مراجعة قبل النشر.

### إضافة مواد ومدرسين وكورسات مستقبلاً

الكيانات المستقلة تجعل إضافة مادة أو مدرس عملية بيانات طبيعية، وتمنع تعديل التطبيق عند كل مادة جديدة. التطبيق يقرأ القيم والعلاقات من Supabase بدلاً من قائمة ثابتة داخل الكود.

### Smart Sync وLocal Database في Student App

يفضل أن تتعامل المزامنة مع معرفات مستقرة (`stage_id`, `subject_id`, `teacher_id`, `course_id`, `lesson_id`) بدلاً من الأسماء النصية. يمكن تخزين snapshots محلية، ومقارنة `updated_at`، وحذف العناصر التي أصبحت غير منشورة، مع الاحتفاظ بتقدم المشاهدة محلياً حسب `lesson_id`.

## 8. المخاطر وكيفية الحفاظ على البيانات

الخطر الأكبر هو تحويل قيمة `instructor` إلى Teacher تلقائياً من دون مراجعة؛ لذلك يجب إنشاء سجل تطبيع ومراجعة القيم قبل الربط. الخطر الثاني هو استخدام `course_categories` كبديل للمواد، لأنه سيخلط التصنيف العام بالبنية الأكاديمية. الخطر الثالث هو جعل FK الأكاديمي إلزامياً قبل ربط الكورسات القديمة، ما قد يمنع القراءة أو الكتابة في Admin Web.

القاعدة الآمنة هي: **إضافة nullable، ثم backfill قابل للمراجعة، ثم dual-read، ثم فرض القيود، وعدم حذف الحقول القديمة قبل التحقق من كل المسارات**. ويجب أن تكون كل Migration قابلة للتراجع منطقياً، مع عدم حذف البيانات الأصلية أثناء فترة الانتقال.

## 9. القرار المعماري المقترح

أوصي بالإبقاء على `course_categories` كتصنيفات عامة، وإضافة طبقة أكاديمية مستقلة مكونة من `stages`, `subjects`, `stage_subjects`, `teachers`, و`teacher_assignments`. يضاف إلى `courses` مفتاح `teacher_assignment_id` nullable في البداية، بينما تبقى `lessons` وحقول YouTube كما هي.

هذا القرار يحقق الرؤية المطلوبة بأقل تغيير على الكيانات العاملة، ويمنع تكرار المدرسين، ويدعم إضافة صفوف ومواد ومدرسين دون تعديل التطبيق، ويهيئ استيراد Playlists وSmart Sync لاحقاً. لكنه يتطلب إدخالاً بشرياً للكورس الحالي لتحديد صفه ومادته، لأن هذه المعلومة غير موجودة في البيانات الحالية.

## 10. حدود هذا التقرير

لم يتم تنفيذ Migration، ولم يتم إنشاء جدول، ولم يتم تغيير أي سجل أو سياسة أو Schema، ولم يتم تعديل AdminStudentWeb أو Student App. التقرير يقدّم خطة قابلة للمراجعة فقط. كما لم يتم الانتقال إلى YouTube Import أو AI Actions.

## الخلاصة

البنية الحالية سليمة كنواة كتالوج صغيرة: `courses` و`lessons` والعلاقة بينهما قابلة للاحتفاظ، و`course_categories` مفيدة كتصنيف عام. لكنها لا تكفي كشجرة تعليمية لأنها تفتقد Stage وSubject وTeacher ككيانات ذات معرفات وعلاقات. أقل Migration آمن هو الإضافة التدريجية مع إبقاء `courses.instructor` و`category_id` فترة انتقالية، وعدم تنفيذ أي ربط تخميني للكورس الحالي قبل مراجعة Admin.
