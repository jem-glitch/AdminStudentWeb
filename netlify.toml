[build]
  command = "pnpm install --frozen-lockfile && pnpm build:web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  EXPO_NO_METRO_WORKSPACE_ROOT = "1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
