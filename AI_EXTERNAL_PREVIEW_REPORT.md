# تقرير مرحلة تحليل الطلبات والموارد الخارجية في Admin AI

## النطاق

تم توسيع `admin-ai` مع الحفاظ على المصادقة الإدارية الحالية، وعدم تغيير Schema أو Student App، وعدم إضافة YouTube Data API. جميع المسارات في هذه المرحلة تحليلية فقط؛ أزرار Preview معروضة بصرياً لكنها غير تنفيذية، ولا توجد عمليات `INSERT` أو `UPDATE` أو `DELETE`.

## ما تم تنفيذه

| المجال | النتيجة |
|---|---|
| Intent Detection | دعم `CHAT` و`PLATFORM_READ` و`EXTERNAL_URL` و`YOUTUBE_PLAYLIST` و`YOUTUBE_VIDEO` و`MIXED`. |
| الروابط | استخراج عدة روابط HTTPS، إزالة علامات الترقيم الطرفية، واستخراج YouTube `video_id` محلياً. |
| YouTube | لا يوجد YouTube Data API ولا API Key. Playlist تُقرأ كصفحة عامة عند الإمكان، وإلا يظهر fallback صريح يطلب لصق الروابط يدوياً. |
| External Fetch | مهلة 12 ثانية، حد أقصى 1 MB، حد redirects، فحص HTTPS وhost وport وcontent-type، ورفض localhost والعناوين الخاصة المعروفة. |
| Supabase | القراءة من الجداول الأكاديمية الجديدة عند طلب بيانات المنصة فقط: `stages`, `subjects`, `stage_subjects`, `teachers`, `teacher_assignments`, `courses`, `lessons`. |
| Mixed Resolution | حل حتمي لعلاقة Stage/Subject/Teacher Assignment وإظهار نتيجتها في Preview. |
| OpenRouter | يرسل فقط السؤال و`request_type` والسياق المرتبط به والموارد الخارجية المختصرة، وليس قاعدة البيانات كاملة. |
| Diagnostics | سجلات allow-listed لا تتضمن الرسالة الكاملة أو Authorization أو JWT أو مفاتيح أو استجابة المزود الكاملة. |

## الاختبارات

| الاختبار | النتيجة | الملاحظات |
|---|---|---|
| CHAT: «مرحبا» | ناجح | عاد رد عربي طبيعي، دون قراءة Supabase. |
| Playlist عامة/غير صالحة | ناجح مع fallback | الرابط الاختباري `PLtest123` أعاد 404، فظهر تحذير واضح وطلب لصق الروابط يدوياً دون اختلاق عناصر. |
| Video مستقل | ناجح | صُنّف نهائياً `YOUTUBE_VIDEO`، وأظهر `video_id` فقط، دون قراءة منصة. |
| عدة روابط Video | ناجح | صُنّف نهائياً `YOUTUBE_VIDEO` وأظهر الرابطين والعنصرين؛ تم الحفاظ على البيانات كما وردت، بما في ذلك تكرار نفس `video_id` عندما ورد بصيغتين مختلفتين. |
| Mixed: Video + الصف/المادة/المدرس | ناجح بعد إصلاح محدود | صُنّف `MIXED`، عاد الرد، وظهرت العلاقة الأكاديمية الموجودة بين الخامس الابتدائي ورياضيات وأحمد علي. |
| Platform Read | ناجح | تم اختبار قراءة الصفوف والمواد والمدرسين والكورسات والدروس في المرحلة السابقة ومقارنتها ببيانات Supabase. |
| 503 في Mixed | تم تحديد السبب وإصلاحه | كان السبب `ReferenceError` لاستخدام `detected` داخل `readRelevantContent` دون تمريره. استُبدل باستخدام `intent` المحلي، ثم نجح الاختبار. |
| Intent خاطئ في Video مستقل | تم تحديد السبب وإصلاحه | كانت كلمات «فيديو/يوتيوب» وحدها تفعّل قراءة المنصة. تم قصر `lesson intent` على درس/دروس/حلقة/حلقات، ثم نجح تصنيف الفيديو المستقل. |

## حالة البيانات الفعلية المستخدمة في الاختبار

ظهرت في لوحة الإدارة بيانات فعلية تشمل الصف **الخامس الابتدائي**، المادة **رياضيات**، والمدرس **احمد علي**. أظهر Preview المختلط أن Teacher Assignment موجود، ولم يتم إنشاء أو تعديل أي ارتباط نتيجة الاختبار. كما بقيت الإحصاءات والكورسات والدروس دون تغيير.

## الأمان والقيود

التحقق من Admin يتم قبل القراءة الخارجية وقبل استدعاء OpenRouter. المفتاح يبقى server-side داخل Edge Function. لا تُرسل مفاتيح أو رؤوس مصادقة إلى النموذج. لا يسمح المسار الحالي بتجاوز حماية YouTube أو تنزيل الفيديوهات أو إنشاء بيانات تلقائياً. External URL لا يُعامل كمصدر موثوق، ولا تُنفذ تعليمات موجودة داخل الصفحة؛ يُستخدم المحتوى المستخرج كسياق محدود للتحليل فقط.

التحقق من العناوين الخاصة يعتمد على فحوص hostname الحالية. إذا أُريد دعم حماية SSRF أقوى في الإنتاج، يُفضّل إضافة DNS/IP resolution server-side قبل السماح بالاتصال، خصوصاً أمام نطاقات قد تشير إلى عناوين خاصة عبر DNS.

## ملفات التعديل

- `supabase/functions/admin-ai/index.ts`
- `components/admin-ai-chat.tsx`
- `todo.md`

نجح `pnpm check` و`pnpm lint` دون أخطاء. بقي تحذير Node غير مؤثر متعلق بغياب `type: module` في `package.json` أثناء تشغيل ESLint، ولم يُعدّل لأنه خارج نطاق المرحلة.

## القرار

المرحلة جاهزة للمراجعة. لم يتم تنفيذ أي Execute أو Import أو Database Write. لا ينبغي الانتقال إلى AI Actions أو Playlist Import الحافظ أو أي Migration قبل موافقة المستخدم الصريحة.
